import 'package:flutter/material.dart';
import 'package:flutteruinew/model/user.dart';

class DrawerWidget extends StatelessWidget {
  User _user = User.init().getCurrentUser();
  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}